# Gitlab Runner

## Pre-Run

* Generate ssh keys in `credentials` section
* Populate `terraform.tfvars` based on its keys values
* `cd terraform`
* `terraform init`
* `terraform plan`
* `terraform apply`
